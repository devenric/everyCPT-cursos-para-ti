# Qué he aprendido con mi proyecto

## IA 
## Programación
    Json.stringify


    | Característica | Python | PHP |
| :--- | :--- | :--- |
| **Definición de Clase**                | `class MiClase:` | `class MiClase { ... }` |
| **Constructor**                        | `def __init__(self):` | `public function __construct() { ... }` |
| **Referencia a Instancia**             | `self.atributo` | `self.metodo()` | `$this->atributo` / `$this->metodo()` |
| **Visibilidad** | Convenciones (`_` protegido, `__` privado). | Palabras clave estrictas (`public`, `private`, `protected`). |
| **Bloques de Código** | Indentación (sangría). | Llaves `{ }`. |
| **Variables** | Dinámicas, sin símbolo especial. | Deben empezar con `$` (ej. `$variable`). |